<%= form_for(@message_field) do |f| %>
  <% if @message_field.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@message_field.errors.count, "error") %> prohibited this message_field from being saved:</h2>

      <ul>
      <% @message_field.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message_id %><br />
    <%= collection_select( :message_field, :message_id, Message.all, :id, :name, {:prompt =>false}) %>
  </div>
    <div class="field">
      <%= f.label :conceptual_field_id %><br />
      <%= collection_select( :message_field, :conceptual_field_id, ConceptualField.all, :id, :name, {:prompt =>false}) %>
    </div>
  <div class="field">
    <%= f.label :projection %><br />
    <%= f.text_field :projection %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
